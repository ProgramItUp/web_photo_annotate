/**
 * Configuration settings for the image annotation application
 */

// Default settings
const DEFAULT_CURSOR_SIZE = 10;
const DEFAULT_BRIGHTNESS = 0;
const DEFAULT_CONTRAST = 0; 

// Recording settings
const BOUNDING_BOX_RECORD_INTERVAL_MS = 100; // Min time between recorded points (ms)
const BOUNDING_BOX_RECORD_PIXEL_THRESHOLD = 5; // Min distance moved between recorded points (pixels) 